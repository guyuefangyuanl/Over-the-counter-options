# 小程序API配置更新 - 问题解决指南

## 🔍 问题现象

小程序控制台显示错误：
```
GET http://localhost:5002/api/v1/groups net::ERR_CONNECTION_REFUSED
```

## ✅ 已完成的修复

我已经更新了配置文件 `miniprogram/config/api.config.js`：
- ✅ 开发环境API地址：`https://flask-ym1v-210758-7-1374336462.sh.run.tcloudbase.com/api/v1`
- ✅ 生产环境API地址：`https://flask-ym1v-210758-7-1374336462.sh.run.tcloudbase.com/api/v1`

## 🚀 需要您执行的操作

### 方法一：重新编译小程序（推荐）

1. **在微信开发者工具中**：
   - 点击顶部菜单「工具」→「编译」
   - 或直接按快捷键 `Ctrl + B`
   - 或点击模拟器顶部的「编译」按钮

2. **清除缓存（可选，如果重新编译后仍有问题）**：
   - 点击「工具」→「清除缓存」→「清除全部缓存」
   - 然后再次点击「编译」

3. **验证修复**：
   - 打开小程序的「报价」页面
   - 打开控制台（Console标签）
   - 应该看到请求地址变为云端API：
     ```
     [API] GET /groups status=200
     ```

### 方法二：重启微信开发者工具

如果方法一无效，尝试：
1. 完全关闭微信开发者工具
2. 重新打开
3. 重新编译小程序

## 📝 验证成功的标志

修复成功后，您应该看到：
- ✅ 控制台不再出现 `ERR_CONNECTION_REFUSED` 错误
- ✅ API请求URL变为 `https://flask-ym1v-210758-7-1374336462.sh.run.tcloudbase.com/api/v1/...`
- ✅ 报价页面能正常加载数据（如果云数据库已有数据）
- ✅ 分组列表能正常获取

## ⚠️ 注意事项

### 如果仍然看到localhost错误

这通常是因为：
1. **缓存问题**：小程序还在使用旧的编译结果
   - 解决：清除缓存 + 重新编译

2. **文件未保存**：配置文件修改未保存
   - 解决：检查 `miniprogram/config/api.config.js` 文件确认修改已保存

3. **模拟器缓存**：模拟器本身的缓存
   - 解决：点击模拟器右上角的「重新载入」按钮

### 检查配置是否正确

在小程序控制台输入以下代码检查：
```javascript
const { getBaseUrl } = require('./config/api.config.js')
console.log('当前API地址:', getBaseUrl())
```

应该输出：
```
当前API地址: https://flask-ym1v-210758-7-1374336462.sh.run.tcloudbase.com/api/v1
```

## 📊 相关文件说明

配置文件位置：
- `miniprogram/config/api.config.js` - API配置中心（✅ 已修改）
- `miniprogram/utils/api.js` - API工具类（使用配置中心）
- `miniprogram/utils/api-group.js` - 分组API（使用配置中心）

所有这些文件都已正确配置，使用统一的配置中心。

## 🎯 下一步

配置更新后，继续完成：
1. ✅ 重新编译小程序
2. ⏳ 导入云数据库数据（按照《云数据库数据导入指南.md》）
3. ⏳ 部署login云函数
4. ⏳ 测试所有功能

---

**当前状态**: 代码已修复，等待重新编译生效 ✨
