# 清空功能性能测试指南

## 快速测试

### 步骤 1：打开行情管理页面
1. 登录管理后台
2. 进入"行情管理"页面
3. 确保已有行情数据（可先上传一些测试数据）

### 步骤 2：执行清空操作
1. 点击"清空所有"按钮
2. 在确认对话框中点击"确定"
3. 观察进度条和耗时

### 步骤 3：记录性能数据

| 数据量 | 预期耗时 | 实际耗时 | 备注 |
|------|--------|--------|------|
| 1,000 条 | < 1 秒 | | |
| 10,000 条 | 2-5 秒 | | |
| 100,000 条 | 10-20 秒 | | |
| 1,000,000 条 | 30-60 秒 | | |

## 性能验证

### 预期表现

✅ **快速响应**
- 进度条应该平稳更新
- 不应该出现长时间卡顿

✅ **准确计数**
- 完成后显示的删除数量应该正确
- 刷新页面后数据应该为空

✅ **错误处理**
- 网络中断时应该有重试机制
- 不应该出现未捕获的异常

### 性能基准

基于微信云数据库 API 性能：

| 场景 | 耗时 | 说明 |
|------|------|------|
| 1,000 条 | < 1 秒 | 单个 API 调用 |
| 10,000 条 | 2-5 秒 | 10 个批次，4 并行 |
| 100,000 条 | 10-20 秒 | 100 个批次，4 并行 |
| 1,000,000 条 | 30-60 秒 | 1000 个批次，4 并行 |

## 故障排查

### 问题：清空速度仍然很慢

**可能原因**：
1. 网络连接不稳定
2. 微信云数据库服务繁忙
3. 数据库索引不完善

**解决方案**：
1. 检查网络连接
2. 在非高峰期重试
3. 联系微信云开发支持

### 问题：清空过程中出现错误

**可能原因**：
1. 云数据库连接中断
2. 权限不足
3. 数据库集合不存在

**解决方案**：
1. 检查云数据库配置
2. 确认用户权限
3. 检查数据库集合是否存在

### 问题：进度条不更新

**可能原因**：
1. 前端 WebSocket 连接问题
2. 后端任务卡住

**解决方案**：
1. 刷新页面重试
2. 检查浏览器控制台错误
3. 查看后端日志

## 性能监控

### 查看后端日志

```bash
# 查看删除操作的日志
tail -f /path/to/logs/app.log | grep "delete_quotes"

# 查看云数据库 API 调用
tail -f /path/to/logs/app.log | grep "云数据库"
```

### 关键指标

1. **API 调用次数**
   - 应该是 `数据量 ÷ 1000` 次
   - 例如：100 万条 = 1000 次 API 调用

2. **平均响应时间**
   - 单个 API 调用：200-500ms
   - 总耗时 = API 调用次数 ÷ 4 × 平均响应时间

3. **并行度**
   - 应该是 4 个并行线程
   - 可以在代码中调整 `max_parallel_deletes` 参数

## 优化建议

### 如果仍然很慢

1. **增加并行度**
   ```python
   max_parallel_deletes = 8  # 从 4 改为 8
   ```

2. **减小批次大小**
   ```python
   batch_size = 500  # 从 1000 改为 500
   ```

3. **增加重试次数**
   ```python
   max_retries = 5  # 增加重试次数
   ```

### 如果出现超时

1. **增加超时时间**
   ```python
   timeout = 15.0  # 从 8 改为 15
   ```

2. **减小批次大小**
   ```python
   batch_size = 500  # 减小批次
   ```

## 反馈

如果性能仍未达到预期，请提供以下信息：

1. 数据量（多少条记录）
2. 实际耗时
3. 后端日志（包含错误信息）
4. 网络环境（是否有代理、VPN 等）
5. 浏览器控制台错误

这将帮助我们进一步优化性能。
