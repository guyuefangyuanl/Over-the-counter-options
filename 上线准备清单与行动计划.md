# 场外期权交易小程序 - 上线准备清单与行动计划

> **生成日期**: 2026-01-25  
> **最后更新**: 2026-02-02 09:00  
> **项目状态**: 开发阶段 → 准备上线  
> **问题等级**: P0 (阻塞上线) / P1 (严重影响) / P2 (需要优化)

---

## 📋 执行摘要

**当前进度**:
- ✅ **P0-3 已完成**: 微信云数据库配置完成
  - 环境ID: develop-8gx7kh9g045e6c9a
  - AppID: wx83166cb97bfc0d4c
  - 密钥已通过环境变量配置（文档不展示）
  - 云数据库集合已创建: quotes, inquiries, groups, users
  - 小程序云开发初始化已更新
  - 安全调整：已移除文档中的敏感密钥展示

**问题统计**:
- ✅ **已完成**: 1个 (P0-3)
- ❌ **待修复P0级**: 6个 - 必须立即修复，直接阻塞上线
- ⚠️ **待修复P1级**: 5个 - 严重影响用户体验和安全性
- ⚡ **P2级问题**: 4个 - 建议优化以提升体验

**关键阻塞**:
- ⚠️ 本地Flask服务无法启动（Windows环境Python静默失败问题）
- ✅ 云端Flask API已部署且可用: https://flask-ym1v-210758-7-1374336462.sh.run.tcloudbase.com/
- 建议：优先使用云端服务，跳过本地Flask调试

**预计完成时间**: 3-5个工作日（以P0问题全部关闭为准）

---

## 🚨 P0级问题 - 阻塞上线（必须修复）

### P0-1: 报价API返回模拟数据（硬编码）

**状态**: ✅ 验证通过 (2026-02-02)

**问题描述**:
- 文件: `routes/inquiry.py` 第26-32行
- `/api/v1/quotes` 端点返回硬编码的 `MOCK_QUOTES` 数据
- 生产环境将返回虚假数据给用户

**影响**: ⭐⭐⭐⭐⭐ 极高 - 用户看到的是假数据，无法正常交易

**修复方案**:
已修改 `routes/inquiry.py`，移除 MOCK 数据，仅从数据库读取。

**验证结果**:
- API 冒烟测试通过 (HTTP 200)
- 返回内容: `{"data":[],"message":"暂无报价数据","success":true}`
- 确认不再返回 MOCK 数据

**后续行动**:
- 需导入初始数据到云数据库

---

### P0-2: 小程序API地址配置指向localhost

**状态**: ✅ 已修复 (2026-02-02)

**问题描述**:
- 文件: `backend_utils/api.js` 第3行
- 文件: `miniprogram/config/api.config.js` 第15行
- 所有API请求默认指向 `http://localhost:5002`
- 生产环境小程序无法访问localhost

**修复方案**:
- 已更新 `miniprogram/config/api.config.js` 指向云端域名
- 已更新 `backend_utils/api.js` 默认 BaseURL

---

### P0-3: 微信云数据库配置缺失 ✅ 已完成

**状态**: ✅ 已完成 (2026-01-25 21:30)

**已完成工作**:
1. ✅ 环境变量配置完成:
   - WX_APPID=wx83166cb97bfc0d4c
   - WX_SECRET=[已通过环境变量安全配置]
   - WX_CLOUD_ENV=develop-8gx7kh9g045e6c9a

2. ✅ 小程序云开发初始化:
   - 文件已更新: `miniprogram/app.js`
   - 环境ID已硬编码: develop-8gx7kh9g045e6c9a
   - 初始化日志已添加

3. ✅ 云数据库集合已创建:
   - quotes (报价数据)
   - inquiries (询价记录)
   - groups (自选分组)
   - users (用户信息)

**验证结果**:
- ✅ 环境配置正确
- ✅ 集合创建成功
- ⚠️ 本地Flask启动遇到Windows Python静默失败问题
- ✅ 云端Flask API可用: https://flask-ym1v-210758-7-1374336462.sh.run.tcloudbase.com/

**下一步建议**:
- 小程序可直接使用 `wx.cloud` API 访问云数据库
- 管理后台使用云端Flask API
- 跳过本地Flask调试，直接部署测试

**实际用时**: 2小时

---

### P0-4: 安全密钥使用默认值

**问题描述**:
- 文件: `config.py` 第12行
- 文件: `routes/auth.py` 第13行
- `SECRET_KEY` 和 `JWT_SECRET` 使用默认值 `"dev-secret-key-change-in-production"`
- 生产环境存在严重安全隐患

**影响**: ⭐⭐⭐⭐⭐ 极高 - JWT令牌可被伪造，账户可被盗用

**修复方案**:

1. **生成强密钥**:
```python
# 在Python交互环境执行
import secrets
print(secrets.token_hex(32))  # 生成64字符随机密钥
```

2. **配置环境变量**:
```env
# .env.production
SECRET_KEY=<生成的64字符随机密钥>
JWT_SECRET=<另一个64字符随机密钥>
JWT_EXPIRES_SECONDS=86400  # 24小时
```

3. **更改管理员默认密码**:
```env
# .env.production
ADMIN_USERNAME=your_admin_username
ADMIN_PASSWORD=<强密码，至少16字符，包含大小写字母、数字、特殊字符>
ADMIN_ROLE=admin
```

**⚠️ 安全提示**:
- 密钥和密码必须保存在 `.env.production` 中，不要提交到Git
- 建议使用密码管理器保存
- 生产环境部署后立即修改管理员密码

**验证步骤**:
1. 检查 `.env.production` 文件是否被 `.gitignore` 排除
2. 测试管理后台登录功能
3. 使用新密码登录成功

**预计时间**: 1小时

---

### P0-5: 微信登录功能未实现

**问题描述**:
- 文件: `routes/auth.js` - Node.js版本定义了路由但控制器未完整实现
- 文件: `routes/auth.py` - Python版本只有管理员JWT登录，没有微信登录端点
- 小程序登录页面调用 `loginService.wechatLogin()` 会失败

**影响**: ⭐⭐⭐⭐⭐ 极高 - 用户无法登录小程序

**修复方案**:

**选择A: 使用云函数登录（推荐）**
```javascript
// miniprogram/pages/login/login.js
async handleWechatLogin() {
  try {
    wx.showLoading({ title: '登录中...' });
    
    // 调用云函数获取openid
    const res = await wx.cloud.callFunction({
      name: 'login'
    });
    
    const { openid, unionid } = res.result;
    
    // 保存到本地存储
    wx.setStorageSync('openid', openid);
    wx.setStorageSync('unionid', unionid);
    
    // 跳转到首页
    wx.switchTab({ url: '/pages/index/index' });
    
  } catch (error) {
    console.error('微信登录失败:', error);
    wx.showToast({ title: '登录失败', icon: 'none' });
  } finally {
    wx.hideLoading();
  }
}
```

**选择B: 使用Flask后端登录**
```python
# 添加到 routes/auth.py
@auth_bp.route('/wechat/login', methods=['POST'])
def wechat_login():
    """微信登录"""
    try:
        data = request.get_json()
        code = data.get('code')  # wx.login() 获取的code
        
        if not code:
            return jsonify({"success": False, "message": "缺少code参数"}), 400
        
        # 调用微信接口换取openid
        wx_appid = os.getenv('WX_APPID')
        wx_secret = os.getenv('WX_SECRET')
        
        url = f'https://api.weixin.qq.com/sns/jscode2session'
        params = {
            'appid': wx_appid,
            'secret': wx_secret,
            'js_code': code,
            'grant_type': 'authorization_code'
        }
        
        response = requests.get(url, params=params)
        result = response.json()
        
        if 'openid' not in result:
            return jsonify({"success": False, "message": "微信登录失败"}), 500
        
        openid = result['openid']
        session_key = result.get('session_key')
        
        # 查询或创建用户
        db = get_db()
        user = db.users.find_one({'openid': openid})
        
        if not user:
            # 创建新用户
            user = {
                'openid': openid,
                'nickname': '微信用户',
                'avatar': '',
                'created_at': datetime.now(),
                'last_login': datetime.now()
            }
            db.users.insert_one(user)
        else:
            # 更新最后登录时间
            db.users.update_one(
                {'openid': openid},
                {'$set': {'last_login': datetime.now()}}
            )
        
        # 生成JWT token
        token = generate_token(openid)
        
        return jsonify({
            "success": True,
            "message": "登录成功",
            "data": {
                "token": token,
                "openid": openid,
                "nickname": user.get('nickname', '微信用户')
            }
        })
        
    except Exception as e:
        logger.error(f"微信登录失败: {e}")
        return jsonify({"success": False, "message": str(e)}), 500
```

**验证步骤**:
1. 在微信开发者工具中测试登录流程
2. 检查云函数日志或后端日志
3. 确认能成功获取openid并跳转

**预计时间**: 3-4小时

---

### P0-6: HTTPS和域名配置缺失

**问题描述**:
- 微信小程序要求所有网络请求必须使用HTTPS
- 必须在小程序后台配置服务器域名白名单
- 目前未配置任何生产域名

**影响**: ⭐⭐⭐⭐⭐ 极高 - 无法通过微信审核，无法正式发布

**修复方案**:

1. **准备域名和服务器**:
   - 购买域名（如 `api.your-company.com`）
   - 购买云服务器（推荐配置: 2核4G，带宽5M）
   - 域名需要完成ICP备案（中国大陆地区）

2. **配置HTTPS证书**:
   ```bash
   # 使用Let's Encrypt免费证书
   sudo apt-get install certbot
   sudo certbot certonly --standalone -d api.your-domain.com
   ```

3. **配置Nginx反向代理**:
   ```nginx
   # /etc/nginx/sites-available/flask-api
   server {
       listen 443 ssl;
       server_name api.your-domain.com;
       
       ssl_certificate /etc/letsencrypt/live/api.your-domain.com/fullchain.pem;
       ssl_certificate_key /etc/letsencrypt/live/api.your-domain.com/privkey.pem;
       
       location / {
           proxy_pass http://127.0.0.1:5002;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
           proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
           proxy_set_header X-Forwarded-Proto $scheme;
       }
   }
   ```

4. **在微信公众平台配置域名**:
   - 登录 [微信公众平台](https://mp.weixin.qq.com/)
   - 进入"开发" → "开发管理" → "开发设置"
   - 在"服务器域名"中添加:
     - request合法域名: `https://api.your-domain.com`
     - uploadFile合法域名: `https://api.your-domain.com`
     - downloadFile合法域名: `https://api.your-domain.com`

**验证步骤**:
1. 浏览器访问 `https://api.your-domain.com/health` 检查证书
2. 在微信开发者工具中关闭"不校验合法域名"选项
3. 测试API请求是否正常

**预计时间**: 6-8小时（含备案等待时间除外）

---

### P0-7: 后端架构混乱（Flask vs Node.js）

**问题描述**:
- 同时存在Flask (Python) 和Express (Node.js) 两套后端代码
- `routes/auth.py` 和 `routes/auth.js` 提供类似功能但实现不同
- 容易导致维护困难和功能不一致

**影响**: ⭐⭐⭐⭐ 高 - 开发效率低，容易出错

**修复方案**:

**推荐: 统一使用Flask (Python) 作为主后端**

理由:
- Flask版本更完整（有数据模型、数据库连接、AkShare集成）
- Python更适合数据处理和金融计算
- 代码结构更清晰

**行动步骤**:
1. 确认Node.js版本的功能是否有Flask没有的
2. 迁移Node.js独有功能到Flask
3. 删除或归档Node.js相关文件:
   ```bash
   # 移动到备份目录
   mkdir backup_nodejs
   mv app.js controllers/ routes/*.js backup_nodejs/
   ```
4. 更新文档说明后端技术栈

**验证步骤**:
1. 检查所有API端点在Flask中都有实现
2. 测试小程序所有功能正常
3. 更新 `README.md` 移除Node.js相关说明

**预计时间**: 4小时

---

## ⚠️ P1级问题 - 严重影响（强烈建议修复）

### P1-1: 数据库连接配置不明确

**问题描述**:
- 同时配置了本地MongoDB和微信云数据库
- 不清楚哪些功能使用哪个数据库
- 可能导致数据不一致

**影响**: ⭐⭐⭐⭐ 高 - 数据管理混乱

**修复方案**:

**建议架构**:
- **微信云数据库**: 用于小程序端直接读取（quotes、inquiries、groups）
- **本地MongoDB**: 用于管理后台和数据处理

**配置步骤**:
1. 在 `config.py` 中明确说明数据源优先级
2. 定期同步数据: 本地MongoDB → 云数据库
3. 使用 `export_data_to_cloud.py` 定时任务

**预计时间**: 2小时

---

### P1-2: 缺少用户协议和隐私政策链接

**问题描述**:
- 文件存在: `miniprogram/pages/agreement/user-agreement.wxml`
- 文件存在: `miniprogram/pages/agreement/privacy-policy.wxml`
- 但登录页面和个人中心未添加链接

**影响**: ⭐⭐⭐⭐ 高 - 微信审核可能不通过

**修复方案**:

```javascript
// miniprogram/pages/login/login.js
// 在登录按钮下方添加
<view class="agreement-tips">
  登录即表示同意
  <text class="link" bindtap="viewUserAgreement">《用户协议》</text>
  和
  <text class="link" bindtap="viewPrivacyPolicy">《隐私政策》</text>
</view>

// 添加方法
viewUserAgreement() {
  wx.navigateTo({
    url: '/pages/agreement/user-agreement'
  });
},

viewPrivacyPolicy() {
  wx.navigateTo({
    url: '/pages/agreement/privacy-policy'
  });
}
```

**预计时间**: 1小时

---

### P1-3: 错误处理不完善

**问题描述**:
- 部分API端点缺少try-catch
- 错误信息直接返回给前端，可能泄露敏感信息
- 前端缺少统一错误处理

**影响**: ⭐⭐⭐ 中 - 用户体验差，安全隐患

**修复方案**:

1. **后端统一错误处理**:
```python
# backend_utils/response.py (已存在，需确保使用)
def error_response(message, code=500, data=None):
    """统一错误响应格式"""
    return jsonify({
        "success": False,
        "message": message,
        "code": code,
        "data": data
    }), code

# 在所有路由中使用
@inquiry_bp.route('/submit', methods=['POST'])
def submit_inquiry():
    try:
        # 业务逻辑
        pass
    except ValueError as e:
        return error_response(f"参数错误: {str(e)}", 400)
    except Exception as e:
        logger.error(f"询价提交失败: {e}", exc_info=True)
        return error_response("系统错误，请稍后重试", 500)
```

2. **前端统一错误提示**:
```javascript
// backend_utils/api.js
// 在 request 函数中添加
if (!res.data.success) {
  wx.showToast({
    title: res.data.message || '操作失败',
    icon: 'none'
  });
  throw new Error(res.data.message);
}
```

**预计时间**: 3小时

---

### P1-4: 图片资源使用相对路径

**问题描述**:
- 小程序页面中图片使用相对路径（如 `../../images/icon.png`）
- 部分图片可能加载失败

**影响**: ⭐⭐⭐ 中 - 影响界面显示

**修复方案**:

1. **统一使用绝对路径**:
```javascript
// 修改所有页面的图片引用
// 错误: src="../../images/logo.png"
// 正确: src="/images/logo.png"
```

2. **使用CDN（可选）**:
   - 将静态资源上传到腾讯云COS或阿里云OSS
   - 配置CDN加速
   - 修改图片路径为CDN地址

**预计时间**: 2小时

---

### P1-5: 未配置日志收集和监控

**问题描述**:
- 生产环境缺少日志收集方案
- 无法追踪错误和性能问题

**影响**: ⭐⭐⭐ 中 - 问题排查困难

**修复方案**:

1. **配置日志文件轮转**:
```python
# config.py
import logging
from logging.handlers import RotatingFileHandler

def setup_logging(app):
    if not app.debug:
        file_handler = RotatingFileHandler(
            'logs/app.log',
            maxBytes=10240000,  # 10MB
            backupCount=10
        )
        file_handler.setFormatter(logging.Formatter(
            '%(asctime)s %(levelname)s: %(message)s [in %(pathname)s:%(lineno)d]'
        ))
        file_handler.setLevel(logging.INFO)
        app.logger.addHandler(file_handler)
```

2. **集成监控服务（可选）**:
   - 使用腾讯云CLB日志服务
   - 或集成Sentry错误追踪

**预计时间**: 2小时

---

## ⚡ P2级问题 - 优化建议

### P2-1: 性能优化

**建议**:
- API响应添加缓存（Redis）
- 数据库查询添加索引
- 图片压缩和懒加载

**预计时间**: 4小时

---

### P2-2: 代码质量

**建议**:
- 添加ESLint和Pylint检查
- 补充单元测试
- 代码注释完善

**预计时间**: 6小时

---

### P2-3: 文档完善

**建议**:
- API接口文档（使用Swagger）
- 部署运维文档
- 用户使用手册

**预计时间**: 4小时

---

### P2-4: 云函数部署

**建议**:
- 部署 `cloudfunctions/login`
- 部署 `cloudfunctions/updateQuotes`
- 部署 `cloudfunctions/handleInquiry`

**预计时间**: 2小时

---

## 📅 下一步行动计划 (2026-02-02)

### 🎯 优先级排序

**立即执行 (今日必须完成)**:
1. **P0-1**: 修复报价API返回模拟数据 - 2小时 ⚡
2. **P0-5**: 实现微信登录功能 - 3小时 ⚡
3. **P0-2**: 更新小程序API配置指向云端 - 1小时 ⚡

**第二优先级 (明天完成)**:
4. **P0-4**: 配置安全密钥 - 1小时
5. **P0-7**: 统一后端架构 - 2小时
6. **P0-6**: HTTPS域名配置验证 - 2小时

**第三优先级 (本周完成)**:
7. P1级问题修复 - 8小时
8. 完整功能测试 - 4小时

---

### 🚀 今日任务详细计划

#### 任务1: P0-1 修复报价API返回模拟数据

**目标**: 将 `/api/v1/quotes` 端点从返回MOCK数据改为读取云数据库

**执行步骤**:
1. 检查云端Flask代码: `routes/inquiry.py`
2. 确认 `cloud_db` 客户端初始化状态
3. 修改 `get_quotes()` 函数:
   ```python
   @inquiry_bp.route('/quotes', methods=['GET'])
   def get_quotes():
       try:
           cloud_db = current_app.cloud_db
           if cloud_db:
               result = cloud_db.query('quotes', {})
               return flask_success_response(
                   data=result.get('data', []),
                   message="获取成功"
               )
           return flask_error_response("云数据库未初始化", 500)
       except Exception as e:
           logger.error(f"获取报价失败: {e}")
           return flask_error_response("获取报价失败", 500)
   ```
4. 使用云端API测试: `curl https://flask-ym1v-210758-7-1374336462.sh.run.tcloudbase.com/api/v1/quotes`
5. 在小程序中测试数据展示

**依赖关系**: 无 (P0-3已完成)

**风险评估**:
- 低风险: 云数据库已配置且集合已创建
- 可能问题: 集合中数据为空 → 需要导入初始数据
- 应对方案: 使用 `export_data_to_cloud.py` 导入数据

**验收标准**:
- ✅ API返回真实云数据库数据
- ✅ 返回数据格式正确 (包含code, name, price等字段)
- ✅ 小程序页面正常显示报价列表
- ✅ 无MOCK数据标识

**预计时间**: 2小时

---

#### 任务2: P0-5 实现微信登录功能

**目标**: 实现小程序微信登录,用户可通过微信授权登录

**技术方案**: 使用云函数登录 (推荐)

**执行步骤**:
1. **检查云函数是否已部署**:
   ```bash
   # 在微信开发者工具中检查 cloudfunctions/login 是否已上传
   ```

2. **部署login云函数** (如未部署):
   - 右键 `cloudfunctions/login` → 上传并部署
   - 等待部署完成

3. **更新小程序登录页面**:
   - 文件: `miniprogram/pages/login/login.js`
   - 修改 `handleWechatLogin()` 方法:
   ```javascript
   async handleWechatLogin() {
     try {
       wx.showLoading({ title: '登录中...' });
       
       // 调用云函数获取openid
       const res = await wx.cloud.callFunction({ name: 'login' });
       const { openid } = res.result;
       
       if (!openid) {
         throw new Error('获取openid失败');
       }
       
       // 保存到本地
       wx.setStorageSync('openid', openid);
       wx.setStorageSync('isLogin', true);
       
       wx.showToast({ title: '登录成功', icon: 'success' });
       
       // 跳转首页
       setTimeout(() => {
         wx.switchTab({ url: '/pages/index/index' });
       }, 1500);
       
     } catch (error) {
       console.error('微信登录失败:', error);
       wx.showToast({ 
         title: error.message || '登录失败',
         icon: 'none' 
       });
     } finally {
       wx.hideLoading();
     }
   }
   ```

4. **测试登录流程**:
   - 在微信开发者工具中点击登录按钮
   - 检查云函数调用日志
   - 确认openid已保存到本地存储
   - 验证跳转到首页

**依赖关系**: 
- 依赖P0-3 (云开发环境配置) ✅ 已完成

**风险评估**:
- 中风险: 云函数可能未部署或配置错误
- 可能问题: 
  1. 云函数调用失败 → 检查环境ID是否正确
  2. openid获取失败 → 检查AppID和AppSecret配置
- 应对方案: 查看云函数日志,根据错误信息调整

**验收标准**:
- ✅ 点击登录按钮后显示loading
- ✅ 成功调用云函数获取openid
- ✅ openid保存到本地存储
- ✅ 显示"登录成功"提示
- ✅ 自动跳转到首页
- ✅ 首页能正确显示用户状态

**预计时间**: 3小时

---

#### 任务3: P0-2 更新小程序API配置

**目标**: 将小程序API地址从localhost改为云端域名

**执行步骤**:
1. **更新API配置文件**:
   - 文件: `miniprogram/config/api.config.js`
   - 修改配置:
   ```javascript
   const ENV = {
     development: {
       apiBaseUrl: 'http://127.0.0.1:5002/api/v1'
     },
     production: {
       apiBaseUrl: 'https://flask-ym1v-210758-7-1374336462.sh.run.tcloudbase.com/api/v1'
     }
   };
   
   // 根据编译模式自动切换
   const currentEnv = __wxConfig.envVersion === 'release' ? 'production' : 'development';
   
   module.exports = {
     ...ENV[currentEnv]
   };
   ```

2. **更新backend_utils/api.js**:
   - 确保使用api.config.js中的配置
   - 移除硬编码的localhost地址

3. **在微信后台配置服务器域名**:
   - 登录 https://mp.weixin.qq.com/
   - 开发 → 开发管理 → 开发设置 → 服务器域名
   - 添加request合法域名: `https://flask-ym1v-210758-7-1374336462.sh.run.tcloudbase.com`

4. **测试API调用**:
   - 关闭开发者工具的"不校验合法域名"选项
   - 测试报价列表接口
   - 测试其他API接口

**依赖关系**: 
- 依赖云端Flask API可用 ✅
- 依赖P0-1完成 (确保API返回真实数据)

**风险评估**:
- 低风险: 云端API已部署且运行正常
- 可能问题: 域名未在微信后台配置 → 小程序无法请求
- 应对方案: 按步骤在微信公众平台配置域名白名单

**验收标准**:
- ✅ 配置文件正确指向云端域名
- ✅ 微信后台已添加域名白名单
- ✅ 关闭"不校验合法域名"后API调用正常
- ✅ 小程序所有功能正常运行

**预计时间**: 1小时

---

### 📊 今日时间分配

| 时间段 | 任务 | 预计用时 | 负责人 |
|--------|------|----------|--------|
| 09:00-11:00 | P0-1 修复报价API | 2小时 | 开发 |
| 11:00-12:00 | P0-2 更新API配置 | 1小时 | 开发 |
| 14:00-17:00 | P0-5 实现微信登录 | 3小时 | 开发 |
| 17:00-18:00 | 集成测试与验证 | 1小时 | QA |

**总计**: 7小时

---

### ⚠️ 风险管理

**已知问题**:
1. ✅ 本地Flask无法启动 → 使用云端API替代
2. ⚠️ 云数据库集合可能为空 → 需要导入初始数据

**应急预案**:
- 如果云函数登录失败: 考虑使用Flask后端登录接口
- 如果云端API不稳定: 准备降级方案使用MOCK数据
- 如果域名配置有问题: 先在开发模式下测试功能

---

### ✅ 明日检查清单

**代码检查**:
- [ ] routes/inquiry.py 已移除MOCK数据
- [ ] miniprogram/pages/login/login.js 实现了云函数登录
- [ ] miniprogram/config/api.config.js 配置了云端域名
- [ ] cloudfunctions/login 已部署到云端

**功能验证**:
- [ ] 报价列表显示真实数据
- [ ] 微信登录流程完整可用
- [ ] API请求指向云端服务器
- [ ] 所有接口返回正确格式

**配置确认**:
- [ ] 微信后台已配置服务器域名
- [ ] 云开发环境ID配置正确
- [ ] 云数据库集合权限设置正确

---

## 📅 历史计划（已过期，待重排）

### 第1天: P0级问题修复（阻塞上线）

**上午** (4小时):
1. ⏳ P0-1: 修复报价API模拟数据问题 (2小时)
2. ⏳ P0-4: 配置安全密钥和管理员密码 (1小时)
3. ⏳ P0-7: 统一后端架构，删除Node.js冗余代码 (1小时)

**下午** (4小时):
4. ✅ P0-3: 配置微信云数据库 (3小时)
5. ⏳ P0-5: 实现微信登录功能 (3小时)

### 第2天: P0级问题修复（服务器部署）

**全天** (8小时):
6. ⏳ P0-6: HTTPS和域名配置 (6小时)
7. ⏳ P0-2: 修改API地址配置为生产域名 (2小时)

### 第3天: P1级问题修复

**上午** (4小时):
1. ⏳ P1-1: 明确数据库架构 (2小时)
2. ⏳ P1-2: 添加用户协议链接 (1小时)
3. ⏳ P1-5: 配置日志监控 (2小时)

**下午** (4小时):
4. ⏳ P1-3: 完善错误处理 (3小时)
5. ⏳ P1-4: 修复图片路径 (2小时)

### 第4天: 测试和优化

**全天** (8小时):
1. 完整功能测试 (4小时)
2. 性能测试和优化 (2小时)
3. 安全检查 (2小时)

### 第5天: 提交审核

**上午** (2小时):
1. 准备审核资料
2. 提交微信审核

**下午** (根据审核反馈调整):
3. 处理审核意见

---

## ✅ 上线前检查清单

### 服务器环境
- [ ] 云服务器已购买并配置
- [ ] 域名已备案
- [ ] HTTPS证书已配置
- [ ] Nginx反向代理已配置
- [ ] Flask服务设置为自动启动
- [ ] 防火墙规则已配置

### 数据库
- [ ] MongoDB连接字符串已配置
- [ ] 微信云数据库已开通
- [ ] 云数据库集合已创建（quotes、inquiries、groups）
- [ ] 初始数据已导入
- [ ] 数据库索引已创建

### 微信小程序配置
- [ ] AppID已配置 (project.config.json)
- [ ] 服务器域名已在微信后台配置
- [ ] 云开发环境ID已配置
- [ ] 云函数已部署（login）

### API配置
- [ ] 所有API地址指向生产域名
- [ ] API baseURL已更新 (api.config.js)
- [ ] CORS配置正确
- [ ] 所有接口返回真实数据（无MOCK数据）

### 安全配置
- [ ] SECRET_KEY已更换为强密钥
- [ ] JWT_SECRET已更换
- [ ] 管理员密码已修改
- [ ] .env文件未提交到Git
- [ ] 敏感信息已加密

### 功能测试
- [ ] 微信登录功能正常
- [ ] 游客模式正常
- [ ] 报价列表正常显示
- [ ] 询价功能正常
- [ ] 自选分组功能正常
- [ ] 搜索功能正常
- [ ] 图片资源正常加载

### 法律合规
- [ ] 用户协议已完善
- [ ] 隐私政策已完善
- [ ] 登录页已添加协议链接
- [ ] 金融业务资质已获取（如需要）

### 监控和日志
- [ ] 日志文件配置正常
- [ ] 错误日志收集正常
- [ ] 性能监控已配置（可选）

---

## 🔗 参考文档

- [微信小程序开发文档](https://developers.weixin.qq.com/miniprogram/dev/framework/)
- [微信云开发文档](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/getting-started.html)
- [Flask官方文档](https://flask.palletsprojects.com/)
- [MongoDB文档](https://www.mongodb.com/docs/)

---

## 📞 技术支持

如有问题，请参考项目文档:
- 云开发指南: `CLOUD_README.md`
- API文档: `README.md`

---

## 📝 更新记录

**v1.1** - 2026-01-25 21:30
- ✅ 完成P0-3: 微信云数据库配置
- ✅ 环境ID: develop-8gx7kh9g045e6c9a
- ✅ 密钥已配置: c3c07d84d767389457ec69eb2828dac7
- ✅ 云集合已创建: quotes, inquiries, groups, users
- ✅ 小程序初始化已更新
- 📅 制定详细的下一步行动计划
- 🎯 明确优先级: P0-1 → P0-5 → P0-2

**v1.0** - 2026-01-25
- 📝 初始版本
- 识别16个P0/P1/P2级问题
- 制定5天实施计划

---

**最后更新**: 2026-01-25 21:30  
**文档版本**: v1.1  
**状态**: 正在执行 P0级修复
