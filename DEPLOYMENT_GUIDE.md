# 账户功能部署与操作指南

## 1. 功能概述
本次更新完成了账户模块的核心功能，包括：
- **用户资料管理**：查看和修改头像、昵称（支持微信头像昵称填写）。
- **持仓展示**：对接真实后端数据，展示持仓列表、资产概览（总市值、总盈亏）。
- **数据隔离**：普通用户只能查看自己的持仓数据，管理员可查看所有。
- **测试数据生成**：提供一键生成测试持仓数据的功能。

## 2. 部署步骤

### 后端部署
1. **更新代码**：确保 `routes/auth.py`, `routes/trade.py`, `app.py` 已更新。
2. **安装依赖**：
   ```bash
   pip install -r requirements.txt
   ```
3. **启动服务**：
   ```bash
   # 确保 MongoDB 已启动
   python app.py
   ```
   服务默认运行在 `http://127.0.0.1:5002`。

### 小程序前端部署
1. **开发者工具**：使用微信开发者工具打开 `miniprogram` 目录。
2. **构建 npm**：点击菜单栏 `工具 -> 构建 npm`（如果依赖有变动）。
3. **上传/预览**：点击预览在手机上查看，或上传代码至体验版。

## 3. 功能验证与测试

### 验证脚本
运行根目录下的 `verify_account_features.py` 脚本可自动验证后端接口：
```bash
python verify_account_features.py
```

### 手动验证流程
1. **登录**：在小程序端点击“微信一键登录”。
2. **查看资产**：进入“账户”页，查看资产概览。如果是新用户，数据可能为空。
3. **生成数据**：
   - **方法A (开发环境)**：在账户页，如果持仓为空，点击“+ 录入”按钮，会弹出提示“是否生成测试数据”，选择“是”。
   - **方法B (API)**：调用 `POST /api/v1/trade/positions/seed` (需 Bearer Token)。
4. **刷新列表**：下拉刷新页面，应能看到生成的3条模拟持仓。
5. **修改资料**：
   - 点击顶部头像，选择微信头像或上传图片。
   - 点击昵称文本框，输入新昵称，点击键盘“完成”或失去焦点自动保存。
   - 再次进入页面，头像和昵称应保持更新状态。

## 4. 安全注意事项
- **权限控制**：已在 `routes/trade.py` 添加 `@require_auth`，确保未登录用户无法获取持仓数据。
- **数据隔离**：普通用户 (`role='user'`) 强制只能查询 `customerId` 为其 `openid` 的数据。
- **环境隔离**：测试数据生成接口仅建议在开发/测试环境使用。

## 5. 常见问题
- **持仓显示为空**：请确认是否已运行生成测试数据，或 MongoDB 中 `positions` 集合是否有该用户的记录。
- **修改头像不生效**：微信 `chooseAvatar` 返回的是临时路径，开发环境可用，生产环境建议配合对象存储服务（OSS/COS）上传后保存永久链接。当前实现仅保存 URL。
