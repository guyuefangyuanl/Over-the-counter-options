<view class="detail-container">
  <view class="header">
    <view class="left" bindtap="goBack">返回</view>
    <view class="title">期权报价</view>
    <view class="tools">
      <view class="tool" bindtap="goCalculator">计算器</view>
      <view class="tool" bindtap="goWorkspace">工作台</view>
    </view>
  </view>

  <view class="search-row">
    <input 
      class="search-input" 
      placeholder="搜索标的名称/代码" 
      value="{{keyword}}" 
      bindinput="onKeywordInput" 
      bindconfirm="onKeywordConfirm"
    />
  </view>

  <view class="product-row" wx:if="{{product}}">
    <view class="product-name">{{product.name}}</view>
    <view class="product-code">{{product.code}}</view>
    <view 
      class="product-change {{product.changePercent > 0 ? 'rise' : (product.changePercent < 0 ? 'fall' : '')}}"
    >
      {{formatChange(product.changePercent)}}
    </view>
  </view>

  <view class="filters">
    <view class="structure-tabs">
      <view 
        class="tab {{structure==='vanilla'?'active':''}}" 
        data-structure="vanilla" 
        bindtap="switchStructure"
      >
        香草
      </view>
      <view 
        class="tab {{structure==='snowball'?'active':''}}" 
        data-structure="snowball" 
        bindtap="switchStructure"
      >
        雪球
      </view>
    </view>
    <view class="term-tabs">
      <block wx:for="{{terms}}" wx:key="*this">
        <view 
          class="tab {{term===item?'active':''}}" 
          data-term="{{item}}" 
          bindtap="switchTerm"
        >
          {{item}}
        </view>
      </block>
    </view>
  </view>

  <scroll-view class="grid-wrapper" scroll-y>
    <view class="grid">
      <view class="grid-header">
        <view class="cell strike">结构/期限</view>
        <block wx:for="{{terms}}" wx:key="*this">
          <view class="cell">{{item}}</view>
        </block>
      </view>
      <block wx:for="{{strikes}}" wx:key="*this">
        <view class="grid-row">
          <view class="cell strike">{{item}}C</view>
          <block wx:for="{{terms}}" wx:key="*this">
            <view class="cell rate">{{getRate(item, index)}}</view>
          </block>
        </view>
      </block>
    </view>
  </scroll-view>
</view>