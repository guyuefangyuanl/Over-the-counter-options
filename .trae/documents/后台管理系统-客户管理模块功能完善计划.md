我将对后台管理系统的“客户管理”模块进行全栈式的功能完善和升级，作为系统升级的示范。

### 后端升级 (Python/Flask)
1.  **修改 `models/customer.py`**:
    *   更新 `get_customers` 方法，增加 `keyword` 参数。
    *   在 MongoDB 查询中实现基于 `$regex` 的模糊搜索（支持按姓名或手机号搜索）。
    *   在云数据库查询中尝试适配相应的模糊搜索语法。
2.  **修改 `routes/customer.py`**:
    *   在 `get_customers` 接口中接收前端传递的 `keyword` 参数，并传递给 Model 层。

### 前端升级 (React/TypeScript)
1.  **完善 `admin-ui/src/pages/CustomerList.tsx`**:
    *   **新增搜索功能**: 添加搜索栏，支持输入关键词过滤客户列表。
    *   **新增客户功能**: 实现“新增客户”模态框 (Modal)，包含姓名、手机号、邮箱、分组等字段的表单验证与提交。
    *   **编辑客户功能**: 实现“编辑”操作，点击后回显客户信息，支持修改并保存。
    *   **优化交互**: 添加操作反馈（成功/失败提示），优化加载状态展示。

通过完成上述工作，客户管理模块将具备完整的 CRUD（增删改查）及搜索能力，达到生产可用标准。后续可参照此模式升级订单、行情等其他模块。